# PROJECT_BRIEF — Bismillah Product Intelligence Platform (MVP v1)

## Objetivo real
Convertir el grafo de conocimiento del catálogo maestro (1.094 productos, 14.915 relaciones, 7 tipos) en una herramienta operativa para el equipo comercial: explorar productos, entender su red de relaciones y extraer recomendaciones accionables (venta cruzada, sustitución, variantes) con gobernanza de confianza.

## Dominio
Distribuidora mayorista peruana con dos universos de catálogo:
- **B — Bienestar** (suplementos, colágenos, herbolaria, gomitas…): 29 subcategorías.
- **F — Farma** (analgésicos, antibióticos, SNC, tópicos…): 26 subcategorías.

## Usuarios
Analistas comerciales, compras y category managers. Uso interno, escritorio primero, responsive.

## Alcance MVP (4 módulos)
1. **Panorama** — salud del grafo: KPIs, distribución por tipo, confianza por tipo, mezcla B/F, hubs, registro de riesgos.
2. **Explorador** — búsqueda por nombre/SKU/tags, filtros por universo, familia, audiencia y tipo de relación.
3. **Producto 360** — ficha con órbita de relaciones (grafo ego interactivo), relaciones agrupadas por tipo con justificación y confianza.
4. **Motores** — simulador de recomendación: venta cruzada, sustitución y variantes, con scoring transparente y exclusión de confianza Baja por defecto (mitigación R-PIG-04).

## Arquitectura
SPA de un solo archivo HTML (vanilla JS + Canvas) por perfil de despliegue, datos embebidos en un `data.js` propio de cada perfil en formato columnar compacto (~450 KB), separados de la lógica de aplicación compartida en `assets/js/app.js`. Sin backend: portable, auditable, cero dependencias de servidor.

Dos perfiles comparten la misma lógica y solo difieren en el dataset: la raíz del repositorio (`assets/js/data.js`, sintético, versionado, seguro para GitHub público — es el perfil por defecto) y `production/` (catálogo real, excluido por completo de git y de su historial — ver README, "Perfiles de despliegue"). El dataset sintético preserva el grafo de relaciones exacto, por lo que todos los KPIs y comportamientos del MVP descritos en este brief son idénticos en ambos perfiles.

## Modelo de datos
- `products[sku, nombre, subcatIdx, grado, tags, audiencias]`
- `rels[srcIdx, dstIdx, tipo(0-6), confianza(0-2), justIdx]` con tabla de 349 justificaciones únicas.
- Subcategoría y audiencia **derivadas de las justificaciones** de MISMA_CATEGORIA / MISMA_AUDIENCIA (cobertura 100 % de subcategoría).
- Desde la Fase 3: datos comerciales (precio, stock, estado, diferencia de precio) por SKU, opcionales y cargados aparte — ver "Fase 3 — Commercial Intelligence" más abajo.

## Supuestos críticos
1. La subcategoría de cada SKU se infiere de sus aristas MISMA_CATEGORIA (fuente única disponible); cobertura verificada: 1.094/1.094.
2. Familias (B1…F10) nombradas por síntesis de sus subcategorías; ajustables.
3. Pesos del motor de venta cruzada (COMPLEMENTA 3.0 > MISMO_BENEFICIO 2.0 > MISMA_AUDIENCIA 1.5 > MISMO_INGREDIENTE 1.0; Alta ×1.0, Media ×0.6) son valores iniciales editables, alineados con la intención comercial de cada tipo.
4. Confianza Baja excluida por defecto en Motores (política R-PIG-04), con interruptor visible.
5. Los 18 productos NO_CLASIFICADO (R-PIG-06) no están en el grafo fuente y quedan fuera del MVP.

## Riesgos heredados
Se integra el registro R-PIG-01…06 del catálogo maestro dentro de Panorama para trazabilidad.

## Criterio de éxito
Un analista encuentra un producto en <5 s, entiende su red en una pantalla y obtiene una lista de venta cruzada justificada sin salir de la herramienta.

## Fase 2 — AI Sales Copilot (en curso)
Paso 1 (aprobado): scaffold visual del panel lateral **AI Sales Copilot** en Producto 360 — layout de dos columnas con panel sticky en la columna derecha, cinco habilidades planificadas (Explicar producto, Comparar productos, Precio y disponibilidad, Mejor alternativa, Venta cruzada inteligente), todas en estado "Próximamente". Es exclusivamente interfaz: sin lógica de negocio, sin llamadas a APIs, sin integración de IA (Gemini u otro proveedor).

Paso 2 (aprobado): módulo **Context Builder** (`assets/js/context-builder.js`) — ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle. Construye, a partir de un producto, el contexto estructurado que el futuro AI Sales Copilot podrá recibir como entrada. Módulo independiente y puro: no depende del DOM ni de `app.js`, no llama a ninguna API ni SDK de IA, no modifica Producto 360 ni el panel del Copilot, y no fabrica datos comerciales — el bloque `comercial` (precio/stock/margen/estado) queda declarado en `null`, listo para poblarse cuando la Fase 1 (Integración de Datos) esté disponible. Verificado con `node scripts/verify-context-builder.js` (10/10 checks) contra el dataset sintético y el real.

Paso 3 (aprobado, commit `5e16c3d`): primera habilidad funcional del Copilot, **Explicar producto**, servida por un **Local Response Provider** (`assets/js/response-provider.js` + `assets/js/providers/local-response-provider.js`) — ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo. Flujo: Usuario → panel AI Sales Copilot → `ContextBuilder.build()` → `ResponseProvider.get().explainProduct(context)` → texto mostrado en el panel. El proveedor sintetiza el texto por reglas a partir, exclusivamente, de campos ya devueltos por el Context Builder (nombre, universo, subcategoría, tags, audiencias, beneficios reales extraídos de justificaciones MISMO_BENEFICIO, resumen de relaciones) — sin IA, sin red, sin datos inventados. El contrato es `async` desde el día uno específicamente para que sustituir este proveedor por uno de Gemini más adelante sea un cambio de una sola línea (`ResponseProvider.use(...)` en `app.js`), sin tocar el panel, el Context Builder ni Producto 360. Verificado con `node scripts/verify-response-provider.js` (9/9 checks, incluyendo los 1.094 productos del catálogo sin excepción) y con `node scripts/verify-context-builder.js` (10/10, sin regresión).

Paso 4 (aprobado, commit `2031a4e`): segunda habilidad funcional, **Comparar productos**. Extiende `ResponseProvider` (`compareProducts` ahora forma parte del contrato exigido) y `LocalResponseProvider` (`assets/js/providers/local-response-provider.js`) — sin archivos nuevos: el algoritmo de comparación vive en el mismo archivo, reutilizando el vocabulario y la extracción de beneficios ya usados por `explainProduct`, sin duplicar lógica. Flujo: Usuario abre la tarjeta "Comparar productos" → busca y elige un segundo producto (Producto B; Producto A es siempre el que ya está abierto en Producto 360) → se construyen **dos** `ContextBuilder.build()` independientes → `ResponseProvider.get().compareProducts(contextA, contextB)` → similitudes y diferencias mostradas en el panel. Todo derivado de campos reales (universo, subcategoría, beneficios, etiquetas, relaciones, incluyendo si existe una arista directa entre A y B en el grafo) — sin IA, sin red, sin datos inventados. Verificado con `node scripts/verify-compare-products.js` (10/10 checks, incluyendo 1.093 pares consecutivos del catálogo sin excepción) y re-verificación de `verify-response-provider.js` (9/9) y `verify-context-builder.js` (10/10) para confirmar cero regresión sobre Explicar producto.

Paso 5 (aprobado, commit `a15d83e`): tercera habilidad funcional, **Mejor alternativa**. Extiende `ResponseProvider` (`bestAlternative` se suma al contrato exigido) y `LocalResponseProvider` — de nuevo sin archivos nuevos, reutilizando `collectBenefits()`/`labelFor()` ya existentes. Flujo: Usuario abre la tarjeta "Mejor alternativa" → se dispara automáticamente (sin selección adicional, igual que "Explicar producto") → **un solo** `ContextBuilder.build()` del producto actual, con `maxPerType` alto para no truncar sus relaciones SUSTITUYE → `ResponseProvider.get().bestAlternative(context)` → producto sustituto, nivel de afinidad y justificación mostrados en el panel. El candidato se elige entre las relaciones SUSTITUYE del producto (la de mayor confianza), excluyendo confianza Baja por defecto — misma política R-PIG-04 que ya aplican Panorama y el motor de "Sustitución" en Motores — y si no hay ningún candidato con confianza suficiente, lo dice honestamente en vez de forzar una recomendación débil. Verificado con `node scripts/verify-best-alternative.js` (10/10 checks, incluyendo los 1.094 productos del catálogo sin excepción) y re-verificación de `verify-response-provider.js` (9/9) y `verify-compare-products.js` (10/10) para confirmar cero regresión sobre las dos habilidades anteriores.

Paso 6 (aprobado, commit `bc8ac33`): cuarta y última habilidad funcional de esta ronda, **Venta cruzada inteligente**. Extiende `ResponseProvider` (`crossSell` se suma al contrato exigido) y `LocalResponseProvider` — sin archivos nuevos, reutilizando `labelFor()` ya existente. Flujo: Usuario abre la tarjeta "Venta cruzada inteligente" → se dispara automáticamente (mismo patrón que "Mejor alternativa") → **un solo** `ContextBuilder.build()` del producto actual, con el mismo `maxPerType` alto ya usado por las dos habilidades anteriores → `ResponseProvider.get().crossSell(context)` → lista de hasta 5 productos complementarios, ordenados por relevancia, con justificación, mostrada en el panel. El criterio de negocio (qué tipos de relación cuentan y cuánto pesa cada uno — COMPLEMENTA > MISMO_BENEFICIO > MISMA_AUDIENCIA > MISMO_INGREDIENTE, ponderado por confianza, Baja excluida por defecto) reaplica, sin duplicar su código, el mismo criterio ya validado en producción por el motor de "Venta cruzada" en Motores (`engineRecos`, pestaña `cross`). Un candidato conectado por varias relaciones elegibles distintas suma su score y aparece mejor rankeado, con ambas señales citadas en su justificación. Si no hay ningún candidato elegible, lo dice honestamente en vez de mostrar una lista vacía sin explicación. Verificado con `node scripts/verify-cross-sell.js` (10/10 checks, incluyendo los 1.094 productos del catálogo sin excepción) y re-verificación de `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10) y `verify-best-alternative.js` (10/10) para confirmar cero regresión sobre las tres habilidades anteriores.

Con este paso quedan implementadas 4 de las 5 habilidades planificadas del Copilot. El paso pendiente (Precio y disponibilidad) depende de la elección de un proveedor de IA real (Gemini u otro) además de la disponibilidad de datos comerciales; requiere su propia especificación (Product/UI/Technical Spec + criterios de aceptación) antes de implementarse. La disponibilidad de datos comerciales pasa a resolverse en la Fase 3 (ver abajo).

## Fase 3 — Commercial Intelligence (en curso)

Paso 1 (aprobado, commit `8cece28`): **integración de datos comerciales reales** al Context Builder — precio, stock, estado y una diferencia de precio (no un margen de utilidad real; ver más abajo). Fuente: un pipeline comercial externo, independiente de este repositorio (Excel → JSON vía un script Python), cuyo dataset ya generado se cruza por SKU con el catálogo real de este proyecto. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo de la arquitectura, el análisis del pipeline externo y las decisiones de diseño.

Componentes nuevos: `assets/js/commercial-data-provider.js` (adaptador — Context Builder solo le pide `getBySku(sku)`, nunca lee el dataset comercial directamente ni conoce su forma original) y `scripts/import-commercial-data.js` (script de importación que lee la salida ya generada del pipeline externo — sin tocarlo ni reemplazarlo — y produce `production/commercial-data.js`, gitignored, con datos comerciales reales y sensibles que nunca se commitean).

Integración 100 % aditiva: `ContextBuilder.build()` sigue devolviendo exactamente el mismo bloque `comercial` de siempre (`disponible:false`, todo en `null`) para cualquier SKU sin cobertura comercial o cuando el proveedor no está cargado (p. ej. el perfil demo público) — verificado ejecutando `scripts/verify-context-builder.js` (el mismo script del Paso 2, sin modificar) sin cargar el nuevo proveedor: **10/10 checks, sin cambios**. Cuando sí hay dato real, se completa `disponible:true` + `precio`/`stock`/`estado`/`priceDifference`; el campo `margen` (margen de utilidad real) permanece `null` siempre — no existe esa fuente todavía, y `priceDifference` (lista − final) nunca se confunde con margen. Verificado con `node scripts/verify-commercial-data.js` (7/7 checks) y contra los datos reales: **cobertura 1.094/1.094** productos del catálogo real. Re-verificación de las cuatro habilidades del Copilot (`verify-response-provider.js` 9/9, `verify-compare-products.js` 10/10, `verify-best-alternative.js` 10/10, `verify-cross-sell.js` 10/10) confirma cero regresión — ninguna de las cuatro se modificó.

Este paso no implementa la habilidad "Precio y disponibilidad" ni modifica ninguna de las cuatro habilidades existentes — solo deja la infraestructura lista.

Paso 2 (aprobado): **quinta y última habilidad planificada del Copilot, "Precio y disponibilidad"** — la primera capacidad funcional que consume explícitamente la infraestructura comercial del Paso 1. Extiende `ResponseProvider` (`priceAndAvailability` se suma al contrato exigido, completándolo con las 5 habilidades) y `LocalResponseProvider` — sin archivos nuevos. Flujo: Usuario abre la tarjeta "Precio y disponibilidad" → se dispara automáticamente (mismo patrón que "Mejor alternativa"/"Venta cruzada") → **un solo** `ContextBuilder.build()` del producto actual (sin necesidad de `maxPerType` alto, ya que el bloque `comercial` no depende de `relaciones.detalle`) → `ResponseProvider.get().priceAndAvailability(context)` → precio final, precio de lista, diferencia de precio, stock y estado mostrados en el panel.

Esta habilidad **nunca** llama a `CommercialDataProvider` ni lee `COMMERCIAL_DATA` directamente — solo lee el bloque `comercial` que `ContextBuilder.build()` ya construyó consultando `CommercialDataProvider` en el Paso 1. `CommercialDataProvider` sigue siendo la única puerta de entrada del dato comercial al sistema; esta habilidad únicamente lo muestra. Si no hay dato comercial para el SKU (incluyendo siempre en el perfil demo público, que nunca carga `COMMERCIAL_DATA`), lo dice honestamente en vez de mostrar algo vacío o inventado.

**Ajuste post-aprobación (observación de revisión de código):** el "precio de lista" ahora prioriza el valor real cuando el pipeline comercial lo provee (`listPrice`, ya existente en esa fuente pero no capturado originalmente); el cálculo derivado (`precio + priceDifference`) pasó a ser únicamente el respaldo cuando no hay valor real. Requirió un ajuste mínimo y aditivo en `scripts/import-commercial-data.js` (capturar el campo) y `context-builder.js` (relay del nuevo campo `comercial.precioLista`) — ni `CommercialDataProvider` ni el pipeline externo se tocaron. Verificado con datos reales: 1.114/1.114 registros comerciales con `precioLista` real, 100 % consistente con el cálculo derivado. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo.

Verificado con `node scripts/verify-price-availability.js` (12/12 checks, cubriendo explícitamente los 3 casos de la especificación y la prioridad real-sobre-derivado, más los 1.094 productos del catálogo real sin excepción) y re-verificación de `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10) y `verify-cross-sell.js` (10/10) para confirmar cero regresión sobre los pasos anteriores.

Con este paso quedan implementadas las 5 de 5 habilidades planificadas del Copilot con un proveedor local. Sustituir `LocalResponseProvider` por un proveedor de IA real (Gemini u otro) sigue siendo, para cualquiera de las 5, un cambio de una sola línea (`ResponseProvider.use(...)` en `app.js`) — requiere su propia especificación antes de implementarse.

## Fase 4 — AI Provider Abstraction (en curso)

Paso 1 (pendiente de aprobación): formaliza, como interfaz nombrada, el contrato de proveedor que `ResponseProvider` ya exigía implícitamente desde la Fase 2. Nuevo módulo `assets/js/response-provider-contract.js` (`ResponseProviderContract.METHODS`/`missingMethods()`/`implementedBy()`); `response-provider.js` deja de mantener su propio array de métodos requeridos y delega en él, sin cambiar su comportamiento ni el texto de sus errores. Nuevo `assets/js/providers/ai-response-provider.js`: implementación placeholder que cumple el contrato pero cuyos 5 métodos rechazan su Promise de inmediato ("todavía no implementado") — sin lógica de IA, sin SDK, sin red. `LocalResponseProvider` no se modificó; su conformidad con el contrato se comprueba dinámicamente en QA, no por inspección. `app.js` sigue activando únicamente `LocalResponseProvider` — `AIResponseProvider` existe como proveedor registrable pero inactivo, preparado para que una fase futura reemplace sus 5 rechazos por llamadas reales sin tocar el resto del sistema. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo.

Verificado con `node scripts/verify-ai-provider-abstraction.js` (10/10 checks nuevos, incluyendo conformidad de ambos proveedores con el contrato y que `AIResponseProvider` nunca resuelve con una respuesta fabricada) y re-verificación completa de las 7 suites anteriores — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10) y `verify-price-availability.js` (12/12) — cero regresión sobre las Fases 2 y 3. Verificado también en navegador (perfil demo): sin errores de consola, `ResponseProvider.get() === LocalResponseProvider` tras la carga, y la habilidad "Venta cruzada inteligente" probada end-to-end sin cambio de comportamiento.

Paso 2 (pendiente de aprobación): **Prompt Context Builder** — nuevo módulo `assets/js/prompt-context-builder.js` que transforma el `Context` de `ContextBuilder.build()` en un `PromptContext` estructurado, determinístico y desacoplado (`productKnowledge`, `commercialContext`, `alternatives`, `crossSell`, `userIntent`), listo para que un futuro `AIResponseProvider` razone sobre él. Es exclusivamente organización de información: no genera texto, no genera prompts, no llama a ningún modelo ni API, no decide qué proveedor usar, y no accede a ninguna fuente de datos por su cuenta — todo sale del `context` recibido como parámetro. `alternatives`/`crossSell` agrupan candidatos reales del grafo por SKU (sin duplicados) y excluyen confianza Baja por defecto (política R-PIG-04, reaplicada una cuarta vez en el proyecto), pero deliberadamente sin puntuarlos ni rankearlos — esa es una decisión de respuesta que sigue siendo exclusiva de `LocalResponseProvider`. `userIntent` nunca se infiere: es exactamente lo que el llamador pase, o `null` si no se pasa nada. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo, incluyendo por qué se reaplica (sin importar) la misma técnica de extracción de "beneficios" ya usada desde el Paso 3 para el nuevo campo "ingredientes", verificada al 100% contra ambos catálogos.

Ningún archivo existente se modificó: Context Builder, `CommercialDataProvider`, el pipeline comercial, `LocalResponseProvider` y `AIResponseProvider` tienen cero diff. El `<script>` se agregó a las tres páginas por consistencia con el Paso 1, sin ningún consumidor que lo active todavía — cero cambio de comportamiento del Copilot, verificado en navegador.

Verificado con `node scripts/verify-prompt-context-builder.js` (17/17 checks, incluyendo ausencia de campos `undefined` mediante un recorrido recursivo propio, ausencia de duplicados en `alternatives`/`crossSell`, y los 1.094 productos del catálogo sintético sin excepción; re-verificado también contra los 1.094 productos reales de `production/data.js`) y re-verificación de las 8 suites anteriores — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10), `verify-price-availability.js` (12/12) y `verify-ai-provider-abstraction.js` (10/10) — cero regresión sobre las Fases 2, 3 y 4 · Paso 1.

Paso 3 (pendiente de aprobación): **Remote Response Provider** — primer consumidor real de `PromptContextBuilder`. Nuevo `assets/js/providers/remote-response-provider.js`: cumple el mismo `ResponseProviderContract` que Local y AI, pero cada método transforma su `context` con `PromptContextBuilder.build()` (sin tocar su API) y lo envía por HTTP a un endpoint configurable — cayendo automáticamente a `LocalResponseProvider` ante *cualquier* error (flag apagado, sin endpoint configurado, `fetch` ausente, fallo de red, HTTP no exitoso, o cuerpo remoto con forma inesperada — incluyendo el caso límite de un `context` inválido que hace fallar la propia construcción del `PromptContext`). Nuevo `assets/js/feature-flags.js` (`FeatureFlags.isEnabled(name)`): no existía ningún mecanismo de feature flag en el proyecto, así que se creó uno mínimo, mismo patrón que `COMMERCIAL_DATA` — un global opcional (`FEATURE_FLAGS`) que ningún perfil de este repositorio define hoy, así que todo flag queda deshabilitado por defecto. La única línea de activación en `app.js` pasa de `ResponseProvider.use(LocalResponseProvider)` a `ResponseProvider.use(FeatureFlags.isEnabled('remoteResponseProvider') ? RemoteResponseProvider : LocalResponseProvider)` — con el flag ausente, resuelve exactamente igual que antes: **cero cambio de comportamiento**. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo, incluyendo por qué el fallback envuelve el intento remoto en `Promise.resolve().then(...)` para capturar también los throws síncronos de `PromptContextBuilder.build()`.

Ningún archivo protegido se modificó: `ContextBuilder`, `CommercialDataProvider`, el pipeline comercial, `LocalResponseProvider`, `PromptContextBuilder` y `AIResponseProvider` tienen cero diff. No existe ningún backend real: `REMOTE_PROVIDER_CONFIG` no está definido en ninguna de las tres páginas — este paso entrega el mecanismo, no un servicio.

Verificado con `node scripts/verify-remote-response-provider.js` (14/14 checks, incluyendo integración del proveedor remoto con un `fetch` simulado inyectado en el sandbox de Node, los 6 modos de fallo que disparan el fallback automático, el caso feliz con una respuesta remota simulada válida, y los 1.094 productos del catálogo sintético con el flag desactivado sin ninguna diferencia frente a Local; re-verificado también contra los 1.094 productos reales de `production/data.js`) y re-verificación de las 9 suites anteriores — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10), `verify-price-availability.js` (12/12), `verify-ai-provider-abstraction.js` (10/10) y `verify-prompt-context-builder.js` (17/17) — cero regresión sobre las Fases 2, 3 y 4 · Pasos 1-2. Verificado también en navegador (perfil demo): sin errores de consola, `ResponseProvider.get() === LocalResponseProvider` tras la carga, y "Venta cruzada inteligente" probada end-to-end etiquetada `local`, sin cambio de comportamiento visible.

Paso 4 (pendiente de aprobación): **Gemini Proxy Server** — primer backend de IA real (Google Gemini, API oficial de Google AI Studio). Antes de implementar se presentó y confirmó una tensión estructural: el proyecto es "sin backend" desde el MVP original, y el requisito de que la API key nunca esté en el frontend solo se puede cumplir introduciendo, por primera vez, una pieza de servidor real. Con esa confirmación, se creó `server/gemini-proxy-server.js`: un servidor Node mínimo sin dependencias externas que expone `POST /copilot`, lee `GEMINI_API_KEY` exclusivamente de `process.env` (nunca del cliente), traduce `{skill, promptContext}` a una llamada real a `generateContent` de Gemini, y normaliza la respuesta a la forma del contrato ya existente. `RemoteResponseProvider` gana únicamente manejo de **timeout** (`AbortController`, configurable) — su único cambio en este paso — y sigue sin saber nada de Gemini específicamente: el endpoint sigue siendo genérico, así que incorporar OpenAI o Claude en el futuro es escribir otro proxy independiente, sin tocar `remote-response-provider.js` ni el pipeline comercial. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo, incluyendo un bug real de QA encontrado y corregido (el sandbox de `vm` no traía `setTimeout`/`AbortController`, causando que un test de timeout colgara el proceso en silencio).

Por decisión explícita del usuario: la QA de este paso permanece 100% simulada (sin `GEMINI_API_KEY` real, sin ninguna llamada real a Google) — se verificó, en cambio, el servidor real (HTTP genuino en un puerto efímero) y un end-to-end real entre `RemoteResponseProvider` y el proxy, con solo la llamada saliente a "Gemini" simulada. Ningún archivo protegido se modificó: `ContextBuilder`, `CommercialDataProvider`, el pipeline comercial, `LocalResponseProvider`, `PromptContextBuilder` y `AIResponseProvider` tienen cero diff. Ningún perfil del repositorio habilita el feature flag ni define `GEMINI_API_KEY` — comportamiento idéntico al actual.

Verificado con `node scripts/verify-gemini-proxy-server.js` (17/17 checks nuevos, incluyendo un end-to-end genuino de HTTP real entre RemoteResponseProvider y el proxy) y `node scripts/verify-remote-response-provider.js` (15/15, con un nuevo check de timeout) y re-verificación de las 9 suites restantes — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10), `verify-price-availability.js` (12/12), `verify-ai-provider-abstraction.js` (10/10) y `verify-prompt-context-builder.js` (17/17) — cero regresión sobre las Fases 2, 3 y 4 · Pasos 1-3. Verificado también arrancando el servidor como proceso real (sin key) con peticiones HTTP reales vía `curl`, y en navegador (perfil demo): sin errores de consola, `ResponseProvider.get() === LocalResponseProvider` tras la carga, "Venta cruzada inteligente" etiquetada `local`, sin cambio de comportamiento visible.

Paso 5 (pendiente de aprobación): **primer flujo conversacional real con Gemini** — antes de implementar se resolvieron tres ambigüedades con el usuario: "conversacional" significa las 5 habilidades ya existentes respondidas por Gemini real (no un chat multi-turno nuevo), la QA sigue 100% simulada (sin key real, sin costo), y el feature flag permanece desactivado en los tres perfiles. Con eso acotado, el trabajo se concentró en dos cosas: **finalizar la construcción del prompt** — extraída a `server/gemini-prompt-builder.js`, con una instrucción de *grounding* específica por habilidad (más allá de la regla genérica de no inventar datos), en particular para "Mejor alternativa"/"Venta cruzada": el modelo debe elegir EXCLUSIVAMENTE entre los candidatos que `PromptContextBuilder` ya filtró según la política R-PIG-04, nunca uno fuera de esa lista — y **ampliar la validación end-to-end** a las 5 habilidades (el Paso 4 solo había probado el flujo completo con una). Se agregó también una segunda capa de defensa server-side en `gemini-proxy-server.js` (`validateGroundedSkuUsage`/`validateAvailabilityConsistency`): si el modelo ignora la instrucción del prompt y devuelve un sku fuera de los candidatos reales, o reporta disponibilidad que el contexto no confirma, la respuesta se rechaza y `RemoteResponseProvider` cae automáticamente a Local — el mismo fallback de siempre, ahora también protegiendo contra una alucinación del modelo, no solo contra errores de transporte. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo.

Ningún archivo servido al navegador (`assets/js/`) se modificó en este paso — solo `server/` y `scripts/` — así que no había ningún comportamiento de UI que pudiera haber cambiado; verificado igualmente en navegador.

Verificado con `node scripts/verify-gemini-prompt-builder.js` (17/17 checks nuevos) y `node scripts/verify-gemini-proxy-server.js` (22/22, con 5 checks nuevos sobre el Paso 4: las dos validaciones de grounding, y la validación end-to-end ampliada a las 5 habilidades) y re-verificación de las 10 suites restantes — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10), `verify-price-availability.js` (12/12), `verify-ai-provider-abstraction.js` (10/10), `verify-prompt-context-builder.js` (17/17) y `verify-remote-response-provider.js` (15/15) — cero regresión sobre las Fases 2, 3 y 4 · Pasos 1-4. **Total: 160/160 checks.** Verificado también arrancando el servidor (ya refactorizado) como proceso real con peticiones HTTP reales, y en navegador (perfil demo): sin errores de consola, comportamiento idéntico al actual.

Paso 6 (pendiente de aprobación): **activación controlada y validación manual con la API real** — antes de implementar se confirmaron dos decisiones con el usuario: la validación real contra Gemini la ejecuta él mismo, en su máquina, con su propia key (no Claude, en esta sesión); y "activación controlada" no deja ningún rastro en el repositorio — ningún perfil define `FEATURE_FLAGS` ni `REMOTE_PROVIDER_CONFIG`. Con eso, el trabajo fue puramente *tooling* y documentación, cero cambios en `assets/js/`. Nuevo `scripts/manual-gemini-live-check.js`, deliberadamente fuera de la convención `verify-*.js` (esa sigue significando "regresión automática 100% simulada"): exige `GEMINI_API_KEY` por variable de entorno (nunca por CLI) y el flag exacto `--confirmo-el-costo` antes de hacer ninguna llamada de red; hace dos llamadas —una HTTP directa al proxy para ver el diagnóstico crudo, otra a través de `RemoteResponseProvider`, el camino real de la app— porque el fallback automático oculta a propósito la causa exacta de un fallo. Se verificó su plomería completa en esta sesión sin gastar nada, con un arnés temporal (no commiteado) que solo simuló la llamada saliente a Google. Nuevo `docs/GEMINI_MANUAL_VALIDATION.md`: guía paso a paso, con una ruta por línea de comandos y una opcional en el navegador (con la advertencia de que cualquier edición de `index.html` para probarlo visualmente debe revertirse, nunca commitearse). Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo.

Verificado con `node scripts/verify-manual-gemini-check-safeguards.js` (6/6 checks nuevos, confirmando por proceso real que el script nunca hace una llamada de red sin key+flag+skill válidos) y re-verificación de las 12 suites restantes — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10), `verify-price-availability.js` (12/12), `verify-ai-provider-abstraction.js` (10/10), `verify-prompt-context-builder.js` (17/17), `verify-remote-response-provider.js` (15/15), `verify-gemini-prompt-builder.js` (17/17) y `verify-gemini-proxy-server.js` (22/22) — cero regresión sobre las Fases 2, 3 y 4 · Pasos 1-5. **Total: 156/156 checks.** Verificado también que ningún `index.html` define `FEATURE_FLAGS`/`REMOTE_PROVIDER_CONFIG` (grep sin coincidencias), y en navegador: comportamiento idéntico al actual.

## Fase 5 — Gemini como proveedor de IA activable (en curso)

Paso 1 (pendiente de aprobación): **AIResponseProvider deja de ser un placeholder** — reemplaza el "todavía no implementado" de la Fase 4, Paso 1 por una implementación real, cumpliendo "Implementar únicamente AIResponseProvider" al pie de la letra: es un delegado puro de una línea por método hacia `RemoteResponseProvider`, sin reimplementar nada. Todo lo que este paso exigía (consumir `PromptContext`, enviar la solicitud, devolver la respuesta de Gemini, fallback automático ante timeout/error HTTP/respuesta vacía/JSON inválido/error del proxy/error del modelo) ya existía, completo y probado, en `RemoteResponseProvider` desde la Fase 4 — reimplementarlo habría sido duplicar lógica ya aprobada. `RemoteResponseProvider` sigue siendo el transporte agnóstico al proveedor; `AIResponseProvider` es la cara pública y estable con la que el resto del sistema conoce "el proveedor de IA". `app.js` no se modificó: la activación real de `AIResponseProvider` en la aplicación queda, deliberadamente, fuera de este paso. Ver [ARCHITECTURE.md](ARCHITECTURE.md) para el detalle completo, incluyendo dos correcciones de comentarios (no de comportamiento) en archivos vecinos que habían quedado desactualizados por este cambio.

Verificado con `node scripts/verify-ai-response-provider.js` (15/15 checks nuevos: delegación exacta en las 5 habilidades, Gemini responde correctamente cuando está disponible, los 6 modos de fallback exigidos uno por uno, sin regresiones en Local, y los 1.094 productos del catálogo sin excepción) y `node scripts/verify-ai-provider-abstraction.js` (11/11, actualizado: la aserción "siempre rechaza" de la Fase 4 se reemplazó por una que confirma que ahora resuelve correctamente) y re-verificación de las 11 suites restantes — `verify-context-builder.js` (10/10), `verify-commercial-data.js` (8/8), `verify-response-provider.js` (9/9), `verify-compare-products.js` (10/10), `verify-best-alternative.js` (10/10), `verify-cross-sell.js` (10/10), `verify-price-availability.js` (12/12), `verify-prompt-context-builder.js` (17/17), `verify-remote-response-provider.js` (15/15), `verify-gemini-prompt-builder.js` (17/17), `verify-gemini-proxy-server.js` (22/22) y `verify-manual-gemini-check-safeguards.js` (6/6) — cero regresión sobre las Fases 2, 3, 4 y 4 · Paso 6. **Total: 172/172 checks.** Verificado también en navegador (perfil demo): sin errores de consola, `ResponseProvider.get() === LocalResponseProvider` sin cambios, "Venta cruzada inteligente" etiquetada `local`, comportamiento idéntico al actual.
